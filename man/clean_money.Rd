% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clean_money.R
\name{clean_money}
\alias{clean_money}
\title{Clean and standardize complex monetary values}
\usage{
clean_money(
  x,
  currency = NULL,
  to = "CAD",
  rates = c(CAD = 1, USD = 1.37, EUR = 1.48)
)
}
\arguments{
\item{x}{A character vector containing monetary values.}

\item{currency}{Optional character vector specifying currency codes for each
element of \code{x}. Used when currency is not embedded in the text.}

\item{to}{Target currency code. Defaults to "CAD".}

\item{rates}{Named numeric vector of exchange rates relative to a base currency.
Must include all currencies present in \code{x} or \code{currency}.}
}
\value{
A numeric vector of monetary values converted to the target currency.
}
\description{
Converts messy monetary strings into numeric values in a target currency.
Handles currency detection, numeric parsing, shorthand multipliers
(e.g., "k", "M", "thousand", "million"), and conditional aggregation of
multiple amounts.
}
\details{
The function follows a hierarchical parsing strategy:
\itemize{
\item If an outer total amount is present (outside parentheses), it is used directly.
\item If no outer total is present, multiple detected amounts are parsed and summed.
\item Currency is detected from the text where possible, otherwise a fallback
\code{currency} vector is used.
\item All values are converted to the target currency using the provided rates.
}
}
\examples{
x <- c(
  "$1.75M CAD ($750k CAD closed, $1M CAD committed)",
  "$250k USD (committed)",
  "$750k CAD closed, $1M CAD committed"
)
clean_money(x)

}
